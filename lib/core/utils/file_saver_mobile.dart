Future<void> saveFile(List<int> bytes, String fileName, String mimeType) async {
  // Mobile file saving requires path_provider package.
  // Add path_provider to pubspec.yaml to enable saving files on mobile.
}
